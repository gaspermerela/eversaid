"use client"

import { motion, AnimatePresence, LazyMotion, domAnimation, useReducedMotion } from "motion/react"

export { motion, AnimatePresence, LazyMotion, domAnimation, useReducedMotion }

// Re-export commonly used motion components for convenience
export const MotionDiv = motion.div
export const MotionSpan = motion.span
export const MotionButton = motion.button
